# 0.44.3 小窗保留修正

开启自动关闭时，只关闭上一个仍展开的竖屏分屏。左上角、右上角的小窗及横屏 App 都保留，不终止后台。

移除了 0.44.2 使用旧方向记录决定关闭的处理。自动关闭前直接检查旧窗口的小窗状态、缩放过渡状态和方向；状态未知时不关闭。观察到退出展开分屏时清除旧窗口候选，避免之后打开其他 App 时误关闭角落小窗。

保留 0.44.2 的动画调整。设置开关含义已同步更新。

请将 NotifyBubbles 文件夹内全部内容覆盖上传，编译成功后安装最新 DEB 并重启桌面。分别测试：展开竖屏 A → B（关闭 A）；A 缩到左/右上角 → B（保留 A）；横屏 A → B（保留 A）；关闭自动关闭开关后 A → B（保留 A）。

本地配置检查通过。新增关闭规则测试随 GitHub Actions 执行，本地 Windows 环境未执行 Apple SDK 编译或真机验证。

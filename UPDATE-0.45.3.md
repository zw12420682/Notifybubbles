# 0.45.3 边缘与角标修正

- 角标缩小并放入图标内部，容器圆角减小；新消息提示改为图标内部轻微缩放，避免左右抖动把角标挤出容器。
- 容器与竖屏分屏窗口留出 8 点间距、与屏幕右边留出 4 点间距。优先按设置大小显示；右侧宽度不足时缩小图标。此调整不改变分屏窗口大小。
- 开始关闭分屏时停止跟随其关闭过程，返回屏幕右边布局。
- 非分屏时，短暂提示结束后图标缩入屏幕右侧一半。最近使用两个 App 仍不进入收纳。
- 键盘弹出时，在原避让区域上再预留一行图标高度；键盘高度不用于缩小图标。
- 保留当前 App 高亮、其他 App 按设置透明度显示、磨砂背景和过渡动画。

注意：图标变小只由窗口右侧空间不足触发，不由图标数量或键盘高度触发。手动把分屏扩到几乎占满屏幕时，无法同时容纳完整图标栏和间距。

上传 NotifyBubbles 文件夹内全部内容，GitHub 编译成功后安装最新 DEB 并重启桌面。本地配置与几何检查不替代 iOS 编译和真机测试。

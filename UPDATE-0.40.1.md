# 0.40.1 深色键盘修正

上一版只覆盖输入框的外观请求与一个键盘配置入口，用户反馈实际键盘仍是浅色。本版同时覆盖键盘宿主视图的深色外观与渲染配置，并由 SpringBoard 发布开关状态，避免 App 沙盒内读不到设置。

切换开关时会更新已出现的键盘宿主，并刷新当前输入视图，不主动结束编辑、不读取输入内容。关闭时恢复宿主原来的外观设置。深色范围仅限键盘区域。

## 上传与测试

1. 将 NotifyBubbles 文件夹里面的所有内容覆盖上传到仓库，包括新文件 Sources/NFBKeyboardState.h。
2. 等最新 GitHub Actions 成功，下载该次运行的 DEB 安装，重启桌面。
3. 关闭并重新打开测试用 App，确保加载新版组件。
4. 在设置中关闭一次“始终使用深色键盘”，再打开。
5. 保持手机浅色模式，先用苹果自带键盘在备忘录测试，再测试微信等 App；检查 App 页面仍为浅色。
6. 关闭开关，检查键盘恢复系统外观；检查中英文、表情和数字键盘。

本地只能检查源码、配置和压缩包，尚未完成 Apple SDK 编译或真机验证。如果仍无效，请说明使用苹果自带键盘还是搜狗等第三方键盘，以及备忘录中是否同样无效。自绘键盘和禁止插件注入的 App 不保证生效。

# 0.41.0 测试版

- 分屏时，一键清理气泡下方新增蓝色旋转气泡，调用 TrollOpen 的 cmd_toggle_orientation 命令。
- 观察到分屏打开或切换到另一个 App 后，等待 0.75 秒避开开场动画，再请求 visualScale=0.86 并定位到左下角，留 8 点边距。
- 每次打开仅定位一次，不持续阻止手动拖动、缩放。
- 保留深色键盘与收纳长按清理功能。

将 NotifyBubbles 文件夹里面的全部内容覆盖上传到仓库，尤其是 Makefile 和 Sources。等待 GitHub 编译成功后安装最新 DEB，重启桌面。

请测试：旋转气泡是否在清理气泡下方；点击是否切换窗口方向；首次打开、关闭后重开、切换 App 时是否位于左下角且缩放为 0.86；手动移动后是否不被持续拉回。

这次使用的是所提供 TrollOpen 1.5.2 二进制中找到的私有接口，尚未真机验证。visualScale 的 setter 是否会立即重排窗口、窗口调度模式是否会再次覆盖位置，都需要手机验证。若大小或位置未生效，请提供截图和 nfb-debug.log；不要把本地配置检查当作功能已验证。

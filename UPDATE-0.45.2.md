# 0.45.2

- 图标始终使用设置中的大小，不因数量、窗口宽度或键盘弹出自动缩小。
- 图标水平居中，容器宽度收窄至图标两侧各 7 点，保留角标空间。
- 图标之间可见间隔由 18 点减至 9 点，隐藏滚动条，仍可上下滑动。
- 清理图标顶部对齐竖屏分屏窗口顶部，容器排列在清理按钮下方。
- 键盘弹出时缩短容器；空间不足时才调整顶部位置以避让键盘。
- 磨砂背景不透明度降到 55%，与图标透明度分开控制。
- 当前分屏 App 图标保持 100% 不透明，其他 App 使用设置中的透明度。清理按钮保持清晰可见。
- 保留移动、显隐和换位动画。

请把 NotifyBubbles 文件夹内全部内容覆盖上传并重新编译安装。本地完成配置和布局计算检查，实际显示及键盘边界仍需手机验证。

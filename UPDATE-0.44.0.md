# 0.44.0

- 横屏分屏不再自动定位左下角、设置 0.86 缩放；气泡栏也不吸附横屏窗口，使用屏幕右侧布局。
- 一键清理位于分屏气泡栏最上方。
- 按实际前台/分屏使用顺序，最近两个仍有气泡的 App 不收纳；新通知不改变使用顺序。使用记录保存在本次桌面进程内，重启桌面后重新记录。
- 点击展开收纳后，5 秒无操作自动收纳，操作会延长计时。最近两个 App 和未读 App 保留。
- 长按收纳只处理实际收纳项目，不清理保留在外面的最近两个 App。

解压后上传 NotifyBubbles 内全部内容覆盖仓库。GitHub Actions 成功后安装最新 DEB 并重启桌面。

本地配置、压缩包检查通过。新增收纳测试由 GitHub Actions 执行，实际横屏判断、窗口跟随及触控仍需手机验证。

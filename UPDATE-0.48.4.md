# NotifyBubbles 0.48.4

边缘与分屏使用同一套图标顺序，切换模式时不再重新把当前 App 插到第一位。现有图标继续使用位置过渡动画，容器保留距底部的滚动距离。

边缘仍显示 3 个图标，可上下滚动；未读图标仍单独显示。分屏将两组按相同先后顺序合并到容器，最多显示 7 个。两种容器高度不同，切换时会按可用空间平滑调整位置。

保留单击同时伸出并打开、缩回时长按拖动、伸出后及分屏长按清除、1 秒停留、键盘旧组件定向清理和 postinst 755 权限修复。基于 0.48 系列，不采用已作废的 0.49。

上传方式：解压后上传 NotifyBubbles 文件夹内的全部内容覆盖，包括 Sources、Preferences、layout、scripts、Tests、.github 和根目录文件。不要将外层文件夹整体套在仓库中。

本地检查不等于 iOS 编译或手机实测，仍需 GitHub Actions 编译并验证切换效果。

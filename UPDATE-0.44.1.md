# 0.44.1 排列修正

0.44.0 桌面采用从下往上的排列，而分屏吸附栏采用从上往下的排列，导致打开分屏后 App 气泡上下颠倒。本版统一使用从下往上的 App 顺序，清理按钮仍位于全部 App 上方。

进入分屏时滚动到列表底部，与桌面的第一个 App 对齐；之后手动上下滑动不被刷新重置。分屏会显示收纳内的 App，所以图标总数可能增加，但原有 App 的相对顺序保持一致。吸附窗口后整栏的屏幕坐标会随窗口改变。

上传 NotifyBubbles 文件夹内全部内容覆盖仓库，编译成功后安装本次运行生成的 DEB 并重启桌面。

已检查 1–49 个 App 的相对排列计算及项目配置。尚未进行 iOS 编译和真机测试。

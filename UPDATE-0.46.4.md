# NotifyBubbles 0.46.4

- 当前分屏变成横屏或小窗后，气泡吸附到最前方仍展开的竖屏窗口；没有合适窗口时返回屏幕右边缘。
- 横屏窗口和小窗保留，竖屏窗口仍按原有自动关闭规则处理，并受设置开关控制。
- 气泡吸附回退不触发窗口关闭，保留现有过渡动画。
- 没有竖屏吸附目标时仍检测当前横屏窗口的方向变化。
- 窗口固定位置逻辑使用窗口本身方向，避免把气泡吸附目标的方向当成当前窗口方向。

解压后将 NotifyBubbles 文件夹内的内容上传到仓库根目录覆盖。
本地配置检查通过；尚需 GitHub 编译和真机验证。

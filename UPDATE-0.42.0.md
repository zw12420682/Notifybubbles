# 0.42.0 分屏贴边图标栏

分屏时，圆角方形 App 图标排列在窗口右边缘，清理和旋转按钮依次在 App 列表后面。其他 App 图标不再暗化。图标栏高度跟随窗口，超出部分上下滑动查看。滑动时后台刷新不主动重置滚动位置。

窗口移动、缩放和旋转时，通过现有窗口观察器更新图标栏位置。右侧空间较小时适当缩小图标；没有足够空间时将图标栏限制在屏幕内，可能与窗口右缘少量重叠。退出分屏后恢复原有屏幕边缘气泡布局和排列方向。

本版保留 0.41.0 的旋转命令及打开后左下角、0.86 缩放请求。它们仍需要 TrollOpen 1.5.2 真机确认。

上传：把 NotifyBubbles 文件夹内全部内容覆盖到 GitHub 仓库，等待 Actions 成功后安装最新 DEB 并重启桌面。

请检查：移动窗口时是否跟随；多 App 时能否上下滑动且正常点击；旋转后是否跟随；退出分屏是否恢复。Windows 本地仅完成配置和布局计算检查，未完成 iOS 编译或真机视觉验证。

# NotifyBubbles 0.48.0

- 双击 App 图标关闭气泡及对应 App 后台，保留气泡破裂动画。单击等待双击判定后执行原来的打开操作，避免双击时先打开 App。
- 边缘模式长按任意 App 图标后上下拖动，容器与外部未读图标整组移动；松手保存位置。普通上下滑动仍用于滚动容器。
- 移除设置里的位置滑块，已有位置作为首次拖动前的默认值。
- 边缘磨砂背景左右各减少 5pt，保留图标触摸与裁剪余量。
- 伸出动画结束后停留 1 秒再收回；拖动和滚动期间保持展开。
- 移除深色键盘开关、代码、注入过滤文件及构建目标。保留键盘弹出时的布局避让。
- 保留竖屏窗口吸附与自动关闭规则。

## 上传与安装

解压后，将 NotifyBubbles 文件夹里的全部内容上传到仓库根目录覆盖，尤其要覆盖根目录 Makefile、Sources、Preferences 和 scripts。
GitHub 中即使还留有旧的深色键盘源文件，新 Makefile 也不再编译它；打包检查会拒绝包含旧键盘 dylib 的安装包。
使用同一插件的新版 DEB 覆盖安装。旧版键盘组件已加载的 App 需退出后重新打开，才能卸载进程中的旧代码。

## 验证

本地配置、设置项、打包检查脚本和压缩包完整性已检查。尚未在本机编译 iOS 插件，手势与拖动效果需 GitHub 编译及手机实测。

# 0.44.2 过渡修正测试版

- 吸附图标栏的位置变化改为短时平滑过渡，不再直接跳变。
- 桌面进入分屏时，图标栏移动、图标布局及滚动位置采用协调的过渡时间。
- 一次分屏期间保持图标大小稳定，避免键盘推动窗口时反复缩放气泡；取消布局回弹。
- 针对“点击角落小窗切换 App 后旧竖屏窗口未关闭”的理解，缓存旧窗口切换前的方向。如果切换时方向暂时变为未知，使用之前已观察到的方向。横屏仍保留。此项不是对角落关闭按钮本身的修改。

上传 NotifyBubbles 文件夹内全部内容，等待 GitHub Actions 成功，安装新 DEB 并重启桌面。

请测试键盘反复弹出收起、桌面点击气泡进入分屏、手动滚动图标栏、通过角落小窗切换 App。若关闭问题仍在，需要操作录屏和 nfb-debug.log 确认具体触发路径。

本地只完成文件配置检查，流畅度和 TrollOpen 行为仍待真机验证。

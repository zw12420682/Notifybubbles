# 0.45.0 分屏磨砂容器

- 分屏 App 图标放在圆角磨砂容器内，最多显示 7 行。超出边界裁切，可上下滑动查看。
- 清理按钮独立在容器上方，不参与滚动。
- 键盘出现时不强制缩小图标，优先上移容器并缩短可见高度；清理按钮一同避让。
- 容器移动、背景淡入淡出、图标排列及清理按钮位置使用可中断的平滑过渡；支持系统减弱动态效果设置。
- 保留小窗、横屏 App 不自动关闭的规则。

若 SpringBoard 收不到第三方 App 键盘的准确边界，则保守预留下方 60% 的屏幕空间；这时可见图标可能少于 7 个。键盘状态识别及避让边界仍需手机验证。

上传 NotifyBubbles 文件夹内全部内容，编译成功后安装最新 DEB，重启桌面。重点检查超过 7 个 App 时的滚动与点击、清理按钮是否固定、键盘弹出和收起时是否遮挡或跳动。

本地只验证配置、压缩包和布局边界计算，未执行 iOS 编译及真机视觉验证。

# 0.40.0 更新与上传

本版本仍然只有一个 NotifyBubbles 安装包。

## 新功能

- 设置 → 通知悬浮气泡 → 始终使用深色键盘。默认关闭，与气泡总开关独立。
- 开启后，系统键盘请求深色外观；不会把整个 App 改为深色。
- 长按收纳图标约 0.45 秒，依次清理收纳气泡和对应后台 App，并播放破裂效果。
- 未读气泡、清理过程中收到新消息的 App，以及当前正在使用的 App 保留。
- 短按收纳图标仍然展开；长按松手不会再次触发展开。

## 上传

解压后打开 NotifyBubbles 文件夹，将里面所有文件和目录上传到原 GitHub 仓库根目录并覆盖同名文件。
不要上传 ZIP 本身，也不要在仓库里再套一层 NotifyBubbles 文件夹。

尤其不要漏掉新文件 NotifyBubblesKeyboard.plist 和 Sources/NFBDarkKeyboard.m，以及更新后的 Makefile、control、Preferences、scripts。
.github 文件夹可以一起上传；本次编译工作流未改变。

## 安装后验证

1. 等 GitHub Actions 成功，下载最新运行的 NotifyBubbles-RootHide 产物，安装其中 DEB 并重启桌面。
2. 关闭并重新打开要测试的 App，让它加载本版本的键盘组件。
3. 保持手机浅色模式，打开深色键盘开关，测试系统键盘的中文、英文、数字和表情界面。
4. 关闭开关，收起后重新打开键盘，检查恢复原外观。若键盘缓存未刷新，重新打开 App。
5. 让两个无未读 App 收纳、另一个 App 保持未读；长按收纳图标，检查只清理收纳项目和对应后台。
6. 清理过程中给某个尚未清理的 App 发新消息，确认新气泡保留；短按仍能展开。

第三方自绘键盘、禁止插件注入的 App 可能不受深色开关影响。
当前已进行本地文件、配置及打包完整性检查；Windows 环境不能执行 iOS 编译或真机验证，以 GitHub Actions 和手机测试结果为准。

# 0.45.3

全部动画统一调校，让移动、伸缩、换位、收纳、破碎更自然流畅。只改动画曲线，不改任何布局/交互行为：

- **气泡移动/伸缩/换位改用弹簧动画**：原 `usingSpringWithDamping:1.0`（临界阻尼，无回弹、像硬滑）改为 `NFBBubbleSpring = 0.85`，展开、收回、上下换位、新气泡入场都带一点 iOS 原生式的轻微回弹落位，读起来更「活」、不生硬。
- **分屏容器整体过渡改用弹簧**：`NFBRailSpring = 0.9`，吸附图标栏在右缘↔分屏窗口之间的切换、以及磨砂背景淡入淡出、圆角 0↔20 的变化都随帧一起平滑落位，不再有圆角/磨砂的瞬间跳变（原先圆角是直接赋值、不参与动画）。
- **气泡破碎动画补缓动**：扩散圆环改 `CurveEaseOut`（起快收慢，像涟漪）；气泡本体缩小淡出改 `CurveEaseIn`（加速塌缩，像被抽走）。
- **提示条淡出补缓动**：`showOpenNotice` 的「接口不可用/请先解锁」提示淡出改 `CurveEaseIn`，收尾更顺。

均继续遵循「系统开启减弱动态时禁用动画」：`UIAccessibilityIsReduceMotionEnabled()` 时弹簧/缓动一律退化为无动画跳变。

请把 NotifyBubbles 文件夹内全部内容覆盖上传并重新编译安装。本地完成配置和布局计算检查，实际动效手感仍需手机验证。

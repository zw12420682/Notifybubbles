# 0.43.0

- 打开分屏后的定位改为屏幕左边缘、底边缘零间距，保留 0.86 缩放请求。
- 移除横竖屏切换气泡。
- 设置新增“切换时关闭上一个竖屏窗口”，默认开启。观察到另一个 App 成为当前分屏后，关闭之前的竖屏窗口，不终止后台。横屏或方向无法确认时保留。
- 保留贴着窗口右侧的可滚动图标栏。

上传 NotifyBubbles 文件夹内全部内容，等待 GitHub 编译成功，安装本次运行生成的 DEB 并重启桌面。

请分别检查：窗口左边与底边是否无空隙；A 竖屏切换到 B 后 A 是否关闭；A 横屏切换到 B 后 A 是否保留；关闭设置开关后是否保留上一个窗口。

本地配置和文件检查通过不等于真机测试通过。TrollOpen 私有接口的实际效果仍需验证；如果同一窗口对象被 TrollOpen 复用，本版不会对该对象执行关闭，以免关闭新 App。

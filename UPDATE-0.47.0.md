# NotifyBubbles 0.47.0

- 边缘模式取消收纳气泡。无未读消息的 App 进入磨砂容器，按最近使用排序，最多同时显示 3 个，其余上下滑动查看。
- 未读消息图标独立显示在容器上方；不在容器内重复。未读清除后按最近使用顺序回到容器。未读过多时该区域也支持滑动。
- 边缘容器缩进屏幕一半。点击或滚动时伸出，展开后停留 2 秒再缩回；滚动期间不收回。
- 分屏仍只吸附展开的竖屏窗口，容器最多显示 7 个，清理按钮独立显示。
- 保留原有分屏自动关闭规则、键盘避让和平滑动画。

上传：解压后上传 NotifyBubbles 文件夹里的全部内容到仓库根目录覆盖。本版增加 Sources/NFBEdgeLayout.h，完整覆盖 Sources、Tests 和版本信息文件。

验证：本地配置检查通过。新增未读分组/恢复顺序/超过三项保留/空列表测试，需由 GitHub 的 macOS 构建运行。未在本机编译 iOS 插件或进行手机实测。
